(include_subdirs unqualified)

(subdir
 text
 (ocamllex
  (modules lexer))
 (menhir
  (modules parser)))

(library
 (name encoding)
 (public_name encoding)
 (modules :standard \ axioms model)
 (libraries core z3)
 (preprocess
  (pps ppx_jane)))
